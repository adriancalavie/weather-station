pre-commit:
  parallel: true
  jobs:
    - name: format
      glob: "*.{css,js,ts,cjs,mjs,d.cts,d.mts,jsx,tsx,json,jsonc}"
      run: bun run prettier --write {staged_files}
      stage_fixed: true

    - name: format package.json
      glob: "package.json"
      run: bun run sort-package-json

    - name: lint
      glob: "*"
      run: bun run biome check --no-errors-on-unmatched --files-ignore-unknown=true {staged_files}

    - name: type check
      run: bun run check-types
